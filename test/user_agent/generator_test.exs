defmodule UserAgent.GeneratorTest do
    use ExUnit.Case, async: true
  
    import UserAgent.Generator
    doctest UserAgent.Generator
  
    test "Need add test" do
      assert true == false
    end
  end
  